<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
      <nav class="navbar navbar-expand-lg navbar-dark bg-dark text-white p-2 mb-5">
      <%= link_to 'Home', root_path, class: 'navbar-brand text-white' %>
      

      <div class="collapse navbar-collapse mx-4 fs-5" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
        <% unless session[:user_id] %>
          <li class="nav-item mx-3">
            <%= link_to 'Register', register_path, class: 'nav-link text-white' %>
          </li>
          <li class="nav-item">
            <%= link_to 'Login', login_path, class: 'nav-link text-white' %>
          </li>
        <% end %>
        <% if session[:user_id] %>
          <li class="nav-item">
            <%= link_to 'Logout', logout_path, class: 'nav-link text-white' %>
          </li>
        <% end %>
        </ul>
      </div>
    </nav>
    <%= yield %>
  </body>
</html>
